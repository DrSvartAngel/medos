services:
  - type: web
    name: medos-pdf-extraction
    runtime: node
    rootDir: server
    buildCommand: npm ci --omit=dev
    startCommand: node pdfServer.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
