/** Interaction geometry, not domain state. Colors come from the current theme. */
export const Interaction = {
  minTarget: 44,
  pressedOpacity: 0.75,
  disabledOpacity: 0.5,
  borderWidth: 1,
} as const;
